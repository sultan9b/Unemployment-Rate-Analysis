# 📊 Проект по анализу динамики безработицы: ETL пайплайн и дашборд

**Анализ исторических данных об уровне безработицы в странах мира с построением автоматизированного ETL-конвейера и интерактивной аналитической витрины.**

- - - 

## 📋 Содержание

- [Цель проекта](#-цель-проекта)
- [Архитектура решения](#-архитектура-решения)
- [Структура данных и пайплайны (DAGs)](#-структура-данных-и-пайплайны-dags)
- [Как запустить проект локально](#-как-запустить-проект-локально)
- [Ключевые выводы и примеры анализа](#-ключевые-выводы-и-примеры-анализа)
- [Автор](#-автор)
---

## 🎯 Цель проекта

Построить автоматизированный **end-to-end пайплайн** (Data Pipeline) для сбора, очистки, трансформации и визуализации данных о безработице, а также создать аналитический инструмент для выявления глобальных и региональных трендов.

### Ключевой вопрос проекта
**Как менялся уровень безработицы в разных странах мира за последние десятилетия и какие глобальные или локальные тренды можно выделить?**

---

## 🏗️ Архитектура решения

Проект реализует классическую трехуровневую архитектуру Data Warehouse с чётким разделением ответственности между слоями.


### Стек технологий
| Категория       | Технологии                                          |
|-----------------|-----------------------------------------------------|
| **Оркестрация** | Apache Airflow                                      |
| **База данных** | PostgreSQL (контейнер Docker)                       |
| **ETL-движок**  | DuckDB (для трансформаций и загрузки)               |
| **Хранилище**   | MinIO (S3-совместимое объектное хранилище для сырых raw данных) |
| **Визуализация**| Metabase                                            |
| **Инфраструктура** | Docker Compose                                      |
| **Языки**       | SQL, Python                                         |

---

## 📁 Структура данных и пайплайны (DAGs)

В базе данных `postgres_dwh` созданы три логические схемы, отражающие этапы движения данных:

1.  **`stg` (Staging)**: Сырые, неизменённые данные. Используется как временный слой для загрузки.
2.  **`ods` (Operational Data Store)**: Очищенные, консолидированные данные. Основная таблица — `ods.fct_unemployment` с историей загрузок.
3.  **`dm` (Data Mart)**: Готовые для анализа данные. Витрина `dm.unemployment_analysis` и материализованные представления (`mv_*`) с расчётными метриками.

---

### Описание пайплайнов Airflow

Процесс оркестрируется тремя последовательными DAG:

1.  **`raw_unemployment_from_owid_to_s3`** (Ежемесячно)
    *   **Задача**: Забирает актуальные данные с портала Our World in Data (OWID).
    *   **Действие**: Сохраняет сырые данные в формате Parquet в бакет MinIO (S3).

2.  **`raw_unemployment_from_s3_to_pg`** (Ежемесячно)
    *   **Задача**: Загружает данные из S3 в оперативный слой хранилища.
    *   **Действие**: Читает Parquet-файлы через DuckDB, проводит базовую очистку и загружает в таблицу `ods.fct_unemployment`.
3.  **`dm_unemployment_analysis`** (Ежемесячно)
    *   **Задача**: Построение аналитической витрины и её визуализация.
    *   **Действие**:
        *   Выбирает последние актуальные данные из ODS.
        *   Рассчитывает бизнес-метрики (категории безработицы, тренды, рейтинги, отклонения от среднего и т.д.).
        *   Выполняет операцию **UPSERT** в витрину `dm.unemployment_analysis`.
        *   Обновляет материализованные представления для дашборда.

---

### Структура дашборда в Metabase

Для BI-системы используется Metabase. Дашборд "Global Unemployment Analysis" состоит из нескольких взаимосвязанных секций:

1.  **Ключевые метрики (KPI)**: Среднемировой уровень безработицы, количество стран, страна с самым высоким уровнем и ее уровень безработицы в %, 
2.  **Динамика** изменения уровня безработицы в странах за последние 34 года, разделение стран на категории по безработице.
3.  **Антирейтинг**: Топ стран с самой высокой безработицей.
3.  **Анализ трендов**:
    *   Линейные графики динамики по странам с проблемным уровнем безработицы (изменение за последние 10 лет).
    *   Линейные графики динамики по всем странам за 34 года.
5.  **Распределение по категориям стран**: Таблица, показывающая сколько стран попадает в каждую категорию безработицы, их статистика.

---

## 🚀 Как запустить проект локально

### Предварительные требования
*   Установленный Docker Compose.
*   Примерно 4 ГБ свободной оперативной памяти.

### Пошаговая инструкция

1.  **Клонируйте репозиторий** и перейдите в его директорию:
    ```bash
    git clone <repository-url>
    cd <project-directory>
    ```
2. **Требуется установить** python версии 3.12 и виртуальное окружение venv, активировать его:
    ```bash
    python3.12 -m venv venv
    source venv/bin/activate
    ```

3. **Запустите инфраструктуру** через Docker Compose:
    ```bash
    docker-compose up -d
    ```
    Будет развёрнуты контейнеры с Airflow, PostgreSQL, Metabase и MinIO.

4.  **Настройте Airflow**:
    *   Откройте Airflow UI: `http://localhost:8080` (логин: `airflow`, пароль: `airflow`).
    *   В разделе **Admin -> Connections** создайте подключения к вашим базам данных и хранилищам (PostgreSQL, MinIO).
5.  **Активируйте DAG**:
    *   В интерфейсе Airflow найдите DAG `raw_unemployment_from_owid_to_s3` и переключите его в активное состояние.
    *   Запустите его вручную или дождитесь scheduled запуска. Остальные DAG запустятся автоматически по цепочке.

6.  **Откройте дашборд**:
    *   Перейдите в Metabase: `http://localhost:3000` и пройдите первоначальную настройку.
    *   Добавьте источник данных — вашу базу `postgres_dwh`.
    *   Перейдите в раздел **Dashboards** и откройте "Global Unemployment Analysis".

---

## 🔍 Ключевые выводы и примеры анализа

![Main](https://github.com/sultan9b/Unemployment-Rate-Analysis/blob/main/Page_1.png)

![Categories](https://github.com/sultan9b/Unemployment-Rate-Analysis/blob/main/Page_2.png)

*   **Глобальный тренд**: Уровень мировой безработицы демонстрирует выраженную цикличность, чётко коррелирующую с глобальными экономическими кризисами (например, 2008, 2020 годы).
*   **Региональные различия**: Страны с развитой экономикой, как правило, имеют более стабильные и низкие показатели, в то время как в отдельных регионах со слабой фиксируются экстремально высокие и волатильные значения.
*   **Эффективность пайплайна**: Проект подтвердил эффективность выбранного стека (Airflow + DuckDB + PostgreSQL) для построения надёжных, идемпотентных и сопровождаемых ETL-процессов, данные дашборда будут обновляться по мере заполнения их в ресурсе Our World In Data. 

---

## 👤 Автор

### **Sultan Buzhkenov**
*   LinkedIn: www.linkedin.com/in/sultan-buzhkenov/
*   GitHub: www.github.com/sultan9b

---

*Этот проект был создан в образовательных целях для отработки навыков Data Engineering и Analytics. А также для демонстрации современных подходов к построению ETL-пайплайнов и систем аналитики. Все компоненты развертываются локально и не требуют облачной инфраструктуры.*

--- 
⭐ Если этот проект был полезен, поставьте звезду на GitHub! *
---
### 💬  Обратная связь

Если у вас есть вопросы, предложения или вы нашли ошибку, пожалуйста:

   - Создайте Issue в репозитории

   - Напишите на email: sbuzhkenov@gmail.com

   - Свяжитесь через LinkedIn